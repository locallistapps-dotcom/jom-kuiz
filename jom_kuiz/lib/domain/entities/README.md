# Domain entities

Plain Dart objects (no JSON annotations, no framework dependencies)
representing core business concepts (e.g. `User`, `Quiz`). These are what the
presentation layer consumes -- never raw DTOs from `data/models`.

No entities are defined yet -- this directory is prepared for future feature
modules.
